-r requirements.txt

# Optional local-experiment extras. NOT imported by app/ - the whole backend,
# post-processing included, runs on numpy alone and the frozen sidecar excludes
# these. Only install them for ad-hoc analysis scripts of your own.
matplotlib==3.10.9
scipy==1.15.3
requests==2.34.2
